# archivo/

**Material deprecado, conservado como historia. No correr.**

Versiones históricas de scripts, superadas por la tubería actual en `scripts/pipeline/`.
Se preserva por trazabilidad del desarrollo; no es reproducible ni parte del dataset.
Cubierto por la regla `archivo/` del `.gitignore` (las copias ya trackeadas persisten
hasta un `git rm --cached`).

## Contenido

- **`scripts_historicos/parsers/`** (7) — parsers previos a `parser.py` 22.0.
- **`scripts_historicos/auxiliares/`** (7) — auxiliares viejos (tiene su propio README).
- **`scripts_historicos/analisis/`** (1) — análisis histórico.
